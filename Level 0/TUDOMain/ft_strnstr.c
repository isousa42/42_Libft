


char	*ft_strnstr(char *str, char *to_find, size_t n)
{
	int i;
	int j;

    i = 0;
	if (to_find[i] == '\0')
		return (str);

    while (str[i] != '\0' && i < n)
    {
        
    }

	return (0);
}
